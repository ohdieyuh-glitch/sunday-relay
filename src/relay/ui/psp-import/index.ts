/**
 * Relay Workspace -> Agents -> Import PSP Agent (website surface).
 *
 * Functional parity with `relay agent import` in the CLI. Both surfaces drive
 * the same shared PSP domain in src/relay/psp.
 */

export {
  RelayPspAgentImport,
  type RelayPspAgentImportProps,
} from './RelayPspAgentImport';
